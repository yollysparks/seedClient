{
  "name": "seedclient",
  "serverURL": "http://localhost:8080/SeedPush/",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "jwt-decode": "^2.2.0",
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "react-image-uploader": "^1.0.0",
    "react-router-dom": "^4.2.2",
    "react-scripts": "1.0.14",
    "react-stars": "^2.2.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  },
  "description": "Front end point for CA3 group6 What we have done so far we can be able to sign up using our seed server,  so the client can connect to the server. all the non authentificated users can see images, we are currently working on fetching the images from the backend where we a can access pictures and save uploaded pictures in our database. =======================================================================================================================================",
  "main": "index.js",
  "devDependencies": {},
  "repository": {
    "type": "git",
    "url": "git+https://github.com/yollysparks/seedClient.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/yollysparks/seedClient/issues"
  },
  "homepage": "https://github.com/yollysparks/seedClient#readme"
}
